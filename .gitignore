# Credenciales - NUNCA subir
.env
ml_tokens.json
estado_sigma.json

# Estado del orquestador: cuando corrio bien cada paso por ultima vez.
# Es local a cada maquina, no se comparte.
estado_pasos.json
estado_ml.json
estado_costos.json

# Log del orquestador: le escribe una linea por corrida, asi que cambia en cada
# maquina al mismo tiempo. Versionarlo garantiza un conflicto en cada git pull.
orquestador_log.txt

# Python
venv/
__pycache__/
*.pyc

# Excel temporales
~$*.xlsx

# Datos sensibles de costos (opcional, si no querés subirlos)
# costos_mensuales/